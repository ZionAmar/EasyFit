<!DOCTYPE html>
<html lang="he">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/layaut.css">
    <link rel="stylesheet" href="/css/calendar.css">
    <link rel="stylesheet" href="/css/table.css">
</head>
<body>
    <header class="main-header">
        <button class="menu-toggle" onclick="toggleMenu()">☰</button>
        <div class="page-title"><%= typeof header !== 'undefined' ? header : "ברוך הבא" %></div>
        <div class="logo">EasyFit</div>
    </header>    

    <% const currentUser = typeof user !== 'undefined' ? user : { name: 'אורח', role: 'none' }; %>
    <div class="layout-container">
        <div class="sidebar" id="sidebar">
            <h1>תפריט</h1>
            <div class="user-info">
                שלום <%= currentUser.name %> (תפקיד: <%= currentUser.role %>)
            </div>
            <nav>
                <ul>
                    <% if (currentUser.name !== 'אורח') { %>
                        <li><a href="/">🏠 דף הבית</a></li>
                        <li><a href="/meetings">📅 מפגשים</a></li>
                        <li><a href="/calendar">🗓 לוח שנה</a></li>

                        <% if (currentUser.role === 'admin') { %>
                            <li><a href="/users">👥 משתמשים</a></li>
                            <li><a href="/rooms">🏠 חדרים</a></li>
                        <% } %>

                        <% if (currentUser.role === 'trainer') { %>
                            <li><a href="/participation">✅ נוכחות</a></li>
                        <% } %>

                        <% if (currentUser.role === 'member') { %>
                            <li><a href="/participation">📌 ההרשמות שלי</a></li>
                        <% } %>

                        <li><a href="#" onclick="confirmLogout()">🚪 התנתק</a></li>
                    <% } else { %>
                        <li><a href="/login">🔐 התחבר</a></li>
                    <% } %>
                </ul>
            </nav>
        </div>

        <div class="main-content">
            <!-- כאן ייכנס התוכן של העמודים -->
